ex:
---
create a django project to takes the input from the user input and to perform the addition of two numbers and to display the result on the webpage by using post()?

step1:
----
to create a project

	django-admin startproject proj7

step2:
-----
goto project location and to create an application in our project location.

	cd proj7

	python manage.py startapp app7

step3:
------
to install our application in the settings.py file of the project.

	settings.py
	-----------
INSTALLED_APPS = [

        'app7'

]

step4:
----
to create a templates and to set the path for our templates in the settings.py file of the project.

	settings.py
	-----------
TEMPLATES = [
    {
        
        'DIRS': [BASE_DIR/'templates'],
    },
]

step5:
-----
to create a views in the views.py file of the application.

	views.py
	--------
from django.shortcuts import render
# Create your views here.
def input(request):
    return render(request,'input.html')

def addition(request):
    a=int(request.POST['fn'])
    b=int(request.POST['sn'])
    c=a+b
    return render(request,'output.html',{'result':c})
    

	input.html
	----------
<html>
<head>
</head>
<body bgcolor="powderblue">
<form action="add" method="post">
Enter First number:
<input type="text" name="fn"><br><br>
Enter Second number:
<input type="text" name="sn"><br><br>
<input type="submit" value="ADD">
</form>
</body>
</html>

	output.html
	-----------
<html>
<head>
</head>
<body bgcolor="powderblue">
<h1>the sum is: "</h1>
{{ result }}
</body>
</html>

step6:
-----
to configure the url patterns for our views in the urls.py file.

	urls.py
	-------
from django.contrib import admin
from django.urls import path
from app7 import views
from django.conf.urls import url

urlpatterns = [
    path('admin/', admin.site.urls),
    path('in/',views.input),
    url('add',views.addition)
]

step7:
------
to apply the migrate

	python manage.py migrate

step8:
-----
to start the server

	python manage.py runserver

step9:
----
to send the requestb to the server

	http://127.0.0.1:8000/in

note:
----
CSRF verification failed

Reason given for failure:
    CSRF token missing or incorrect.

what is CSRF?
-------------
	CSRF(Cross-Site Request Forgeory) is an attacker.

	we can prevent your web applications from these attacker we are adding csrf_token template tag in the html file whenever we are using post method.

	{% csrf_token %}

modified input.html
	 -----------
<!DOCTYPE html>
<html>
<head>
</head>
<body bgcolor="powderblue">
<form action="add" method="post">
 {% csrf_token %}
Enter First number:
<input type="text" name="fn"><br><br>
Enter Second number:
<input type="text" name="sn"><br><br>
<input type="submit" value="ADD">
</form>
</body>
</html>

structure:
---------
	proj7
	   |--->app7
	   |      |---> views.py
	   |--->proj7
           |      |---> setting.py
	   |      |---> urls.py
           |--->templates
           |      |---> input.html
	   |      |---> output.html
           |---> manage.py

assignment:
---------
create django project to implement keyword checker application?

assignment2:
----------
create django project to generate abrivated names for the given input?

assignment3:
------------
create a django project to generate multiplication table?
























